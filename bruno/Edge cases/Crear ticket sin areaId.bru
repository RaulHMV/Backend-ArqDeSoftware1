meta {
  name: Crear ticket sin areaId (400)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/tickets
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{idToken}}
  Content-Type: application/json
}

body:json {
  {
    "title": "Sin area",
    "description": "No mando areaId"
  }
}

assert {
  res.status: eq 400
}
