meta {
  name: Comentario sin content (400)
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/tickets/{{ticketId}}/comments
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{idToken}}
  Content-Type: application/json
}

body:json {
  {
    "type": "PUBLIC"
  }
}

assert {
  res.status: eq 400
}
